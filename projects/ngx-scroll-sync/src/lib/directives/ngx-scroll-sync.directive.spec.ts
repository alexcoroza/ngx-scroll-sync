import { NgxScrollSyncDirective } from './ngx-scroll-sync.directive';

describe('NgxScrollSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxScrollSyncDirective();
    expect(directive).toBeTruthy();
  });
});
